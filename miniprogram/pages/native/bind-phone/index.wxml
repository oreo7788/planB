<!--pages/native/bind-phone/index.wxml-->
<view class="container">
  <view class="header">
    <view class="icon-wrapper">
      <image src="/images/phone.png" mode="aspectFit"></image>
    </view>
    <text class="title">绑定手机号</text>
    <text class="desc">绑定手机号后，换机时可找回账号数据</text>
  </view>
  
  <view class="actions">
    <button 
      class="btn btn-primary" 
      open-type="getPhoneNumber" 
      bindgetphonenumber="onGetPhoneNumber"
      loading="{{loading}}"
      disabled="{{loading}}"
    >
      授权手机号
    </button>
    <button class="btn btn-default" bindtap="onBack">暂不绑定</button>
  </view>
  
  <view class="tips">
    <text class="tips-title">说明：</text>
    <text class="tips-item">• 手机号仅用于账号找回</text>
    <text class="tips-item">• 一个手机号只能绑定一个账号</text>
    <text class="tips-item">• 绑定后可在设置中更换或解绑</text>
  </view>
</view>
